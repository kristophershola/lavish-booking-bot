def check_apartment_availability(unit, date):
    """Phase 3 work. Reads a specific unit tab (A1 through C2), finds the
    row matching the requested date, and checks the AVAILABILITY column
    for a 'BOOKED' value. Read-only, never writes to the sheet.
    """
    raise NotImplementedError("Booking engine not yet built, see Phase 3 in project plan.")


def check_cinema_session_availability(hall, date, session_index):
    """Phase 3 work. Reads the HALL 1 or HALL 2 tab, finds the row for the
    requested date, and checks the relevant session column for a booking
    shortcode or an 'XTRA TIME' marker. Read-only, never writes to the sheet.
    """
    raise NotImplementedError("Booking engine not yet built, see Phase 3 in project plan.")
