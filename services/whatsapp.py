# Not in current project scope.
#
# Lavish's current channel is Instagram DM only (see services/instagram.py).
# This file exists as a placeholder in case a WhatsApp channel is added
# later, following the same pattern: a webhook receiver, a send function,
# and a check for the bot's own outgoing messages.
